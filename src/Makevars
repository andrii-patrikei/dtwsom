# Used when the folder is built as an R package (R CMD INSTALL). For
# Rcpp::sourceCpp() the [[Rcpp::plugins(openmp)]] line does the same job.
CXX_STD = CXX17
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS) -O3
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS)
